{
  "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "resourceBaseName": {
      "value": "bot-msteams-bot-${{TEAMSFX_ENV}}"
    },
    "botAadAppClientId": {
      "value": "${{BOT_ID}}"
    },
    "botDisplayName": {
      "value": "bot-msteams-bot-${{TEAMSFX_ENV}}"
    },
    "botAppDomain": {
      "value": "${{BOT_DOMAIN}}"
    },
    "botAddAppClientSecret": {
      "value": "${{BOT_PASSWORD}}"
    }
  }
}